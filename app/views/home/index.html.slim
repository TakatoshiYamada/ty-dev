/ app/views/home/index.html.slim

/ 画像を一枚表示する
/ 中央に表示し、上下の余白を設ける
/ 写真は4*5の比率で編集されているので、それに合わせる
/ デバイスの幅に合わせて画像を縮小する
.container.mx-auto.my-10
  = image_tag @user.site.top_image.variant(resize_to_fill: [500, 400]), class: "w-full"